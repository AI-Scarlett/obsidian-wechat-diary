All cited line numbers check out against the working-tree `main.js`. Here is the merged report.

# 「#15 路径可配置 + 写进已有的每日笔记」审查汇总

对象: 插件仓库工作区 工作区未提交改动(main.js / tests/bindtest.js / README.md / docs/ / styles.css), 对照终稿 `docs/15-daily-note-mode-draft.md`。`node tests/bindtest.js` 529 条全过, 下面所有问题都是测试桩看不到的。行号均为工作区 main.js。

## A 必修

**A1 [S1] 消息里有未闭合代码围栏 → 节吞掉后面的用户内容, 「撤回」删用户段落、封存行写到用户内容之后。** 共用模式、节固定在文件中间(模板 `## 微信随手记` 后接 `## 复盘` `## 待办`)。用户发一条含奇数个 ``` 的消息(贴命令、贴日志、AI 回答被截断都会), `_appendBlock`(3748)只调 `escapeHeadingLines`(3409, 只管 `^#{1,6} `), 围栏行原样落进节; 之后 `locateSection`(3342–3368)扫到它 `inFence` 翻转(3355), 用户的 `## 复盘` `## 待办` 不再算终止标题, `bodyEnd` 延到文件末尾。实测: 写入后 countDay 把用户两段数进去; 「撤回」返回 removed=`## 待办\n- 买菜`, 用户的待办被删; 再发一条落在用户内容之后; 「晚安」封存行写在用户复盘之后; 写回还会把用户段落的 CRLF/尾空白一并规整。直接违反「不动用户内容」。终稿 §4.2 只要求转义 `#` 行, 是终稿漏洞, 实现照做。**修法**: 共用模式 `escapeHeadingLines` 同时把 `^( {0,3})(```|~~~)` 改成 `$1\$2`(`\``` 在 CommonMark 是字面反引号, 不匹配 3321 `FENCE_LINE_RE`); 可只在块内围栏行数为奇数时转义(偶数=自闭合, 用户发完整代码块仍能渲染), 最稳是与 `#` 同款全转义。终稿 §4.2 补一句, 契约 v1.4 加「行首 `\``` 视为原文」。bindtest 加: 单行 ``` 消息 → 节仍在下一用户标题截断、撤回只删自己的块、节外字节不变; 成对 ``` → 节边界正确。

**A2 [DOC-1] Templater 复核只保护当天第一块, 窗口内连发的第 2/3 条静默丢, 回执已说「记下来啦」。** 共用模式+模板+Templater 新建触发器。`_createDayFile` 3720 `if (body) this._scheduleCreateCheck(day, body)` 只登记 create 时那一块, 3738 `present` 只 includes 这一块, 找到即 return, 连 console.warn 都没有。实跑: 第一条 create → 300ms Templater read 快照 → 600ms 第二条写入(回执「今天第 2 段」) → Templater 整篇写回 → 复核见第一条在, 直接返回; 最终文件只有第一条, countDay=1, 第二条永久丢失、无任何提示。README:50「丢了就补回」和终稿 §4.3(对策正是为第 2/3 条设计的)都不成立; 变体(folder template 整篇覆盖)也只补回第一条。慢模板(tp.web/tp.system.prompt)窗口是秒级, 终稿自己承认。**修法**: 复核对象改成「本次 create 后、复核前写进当天节里的所有块」——`_createDayFile` 时记 `this._pendingCheck = { day, gen, items: [{timestamp, block}] }`; `_appendBlock` 共用路径 `_editDay` 成功后若 `_pendingCheck.day === day` 则 push 并重置 1.5s 计时器(连发只复核一次); 复核时逐条 `!sectionBody.includes(item.block)` 的按原顺序 `_appendBlock` 补回(同分钟自然并段), 补写期间不再 push; 补回后 Notice「Templater 覆盖了 N 条, 已补回」让失败可见。B11 加「create 后连写两条 → 快照写回 → 两条都在」。若本轮不改代码, README:50 必须改成「只补回第一条」——但那违反产品原则, 建议改代码。

**A3 [Z1] 外来文件护栏命中 0.3.1 自己产出的文件: 「打开今天的日记」建的空文件被用户先手写过 → 0.4.0 拒绝撤回、段数变、回执说假话。** 0.3.1 的 `open-today-note` 只 `openLinkText`, 文件不存在时由 Obsidian 建空文件(终稿 §4.6 自己写明), 0.4.0 独立分支(5363–5372)没改, 会继续产出。用户早上用命令打开今天的文件敲了任何东西(哪怕一个回车), 之后第一条微信到达: `_transform`(3616)对已存在文件走 process, 3752 `if (existing)` 非空就不写 frontmatter——两版落盘字节相同(HEAD 与工作区并排差分脚本证实: 预置 `"\n"`、`"   \n"`、`"# 2026-08-20\n\n用户内容\n"` 三种形态)。之后差异出现: (a) 「撤回」3875 `isForeignFile`(3431, 以 frontmatter 有无 source 为唯一判据)→ `{foreign:true}` → 回 `UNDO_FOREIGN_REPLY`(2613)「这个文件不是插件建的, 撤不了」——文件正是插件命令建的, 被拒的是插件刚写的块, 0.3.1 能撤, 且当天之后每次撤回都被拒; (b) write/writeImage/writeAttachment 回执段数从「第 2 段」变成「第 1 段」并带「已经记录在新开的文件里啦」(文件并非新开); (c) 「在吗」同变。标准 0.3.1 文件(含 CRLF 头、属性面板重写头、带引号 source、019 Python 头)全部逐字节一致。终稿 §4.2 末段「默认布局的老用户文件都有 frontmatter, 永不触发」与 §9 B1 前提不成立。**修法**: 撤回口径与 3636 `_count` 同源——3875 改成 `if (isForeignFile(content)) { const i = content.search(HEADER_RE_G); if (i < 0 || !countMessages(content.slice(i))) { foreign = true; return content; } }` 然后照常 `removeLastBlock(content)`(段头之后有块时全文最后一个块必在段头之后, 只删它和孤儿段头, 段头之前的用户内容原样保留; 脚本证实与 0.3.1 撤回结果字节一致)。`UNDO_FOREIGN_REPLY` 改成不断言归属: 「这个文件里没有微信记的内容可撤, 请到 Obsidian 里手动删」。终稿 §4.2/§9 B1 与 00-decisions D13 第 10 条把「永不触发」改成「命令建的空文件被用户先手写过的会触发」; bindtest【H】B12 加 `"\n"` 与「用户先写一段」两形态, 断言写入字节同 0.3.1、能撤插件块、再撤被拒且用户内容字节不变。

**A4 [S3, 3] 共用模式 + 附件位置「跟随 Obsidian」: 插件建的壳文件没有 1.5s 复核, 第一条是图片/文件/语音原声时块被 Templater 抹掉无补救。** `resolveAttachmentPath` 3521 调 `_createDayFile(day, "")`, 3720 因 body 空不登记复核; 随后 createBinary、3831–3832 `_appendBlock` 走 process 追加块。实跑(有模板/无模板各一次): created=[当天文件, 附件], timers=0; 对照文字先到 timers=1。模拟 Templater 300ms 后整篇 modify: diary 模式图片块被补回, obsidian 模式文件只剩模板, 图片块没了, 回执「图片收好啦」, 图片文件留在附件目录没人知道。大图/视频 createBinary 几百 ms、慢模板窗口秒级, 正落在 read→modify 窗口内。终稿 §4.3「只在本次由插件建了文件这条路径上复核」——这条路径插件确实建了文件却漏了, 而它正是 Roy 这类「附件有专门文件夹」用户的第一张图。**修法**(与 A2 合并做): `resolveAttachmentPath` 建壳成功后记 `this._freshShellDay = day`(或 `_lastCreated = {path, ts}`); `_appendBlock` 共用分支 `_editDay` 返回后若命中且 <1500ms, 调 `_scheduleCreateCheck(day, out)`(此时节不存在, fn("") 产出的 out 与 create 路径 body 同形)并清掉标记; 采纳 A2 的 pending 列表后直接 push 进去即可。bindtest B13 obsidian 用例加 `hTimers.length === 1` 与模拟覆盖后补回断言。

**A5 [S2] 复核被无关的「撤回」/「晚安」作废: 第 1 条回执「记下来啦」却永久丢失; 反过来节刚被抹掉时「撤回」撤不了、1.5s 后块又被补回。** `_scheduleCreateCheck` 3729 记 gen、3733 `_mutGen !== gen` 直接 return; 3886 撤回成功、3917 真封存各 +1, 与撤的是不是这个块无关。实跑三组: (1) 第一条 create → 「晚安」sealed(_mutGen=1) → Templater 整篇写回 → 复核放弃, 第一条和封存行全没了, 回执却是「记下来啦」+封存成功——Obsidian 重启后追平积压时「hi + 晚安」在同一批次毫秒内顺序处理(5835 `for (const msg of msgs) await this._handleIncoming`), 不苛刻; (2) 第 2 条 → 撤第 2 条 → 复核放弃 → 第 1 条丢; (3) 节被抹掉瞬间「撤回」→ 找不到节 ok:false → _mutGen 不变 → 复核把用户想撤的块补回, 得再撤一次。实现与终稿 §4.3「它们各自 _writeGen++ 则复核作废」措辞一致, 是终稿把「防补回已撤的块」写成了「任何撤回/封存都作废」——产品驳斥者认为属记录在案, 但场景 (1) 丢内容且回执失实, 我按后果归 A, 由谷雨拍板(见末节)。**修法**: 作废条件改为「被撤的正是这份 body 里的块」——`undoLastBlock` 成功时记 `plugin._lastRemoved = removed`, 复核回调用 `plugin._lastRemoved && body.includes(plugin._lastRemoved)` 才放弃(采纳 A2 后改成从 pending 列表里移除该块); 封存不作废(补回正文后若本日 sealed 过再 append 封存行); 共用模式 undo 因「无节」返回 ok:false 时置 `_undoWantedDay = day`, 复核见之放弃。B11 加三条反例。先改终稿 §4.3 措辞再改代码。

**A6 [Z2] finalizeDay 对没有任何插件内容的外来文件照样写封存行并回「已自动收尾」, 与「在吗」=0 段自相矛盾。** B1 配置(开关关、pathFormat 指向用户每日笔记)的日常路径: 用户昨天只在自己的每日笔记里写了三段没发微信, 今早第一条到达 → `_loadOrReset`(4289–4301) → `finalizeDay` 独立分支(3893–3918, 无 `isForeignFile` 判断) → `sealContent`(3477)用 `countMessages` 全文数得 n=3 → 往用户昨天的文件末尾追加 `\n\n---\n_(今日封存于 09:15)_\n`, 回执「(昨天的已自动收尾)」。同一文件「在吗」走 `_count`(3636)回 0 段, 「晚安」`signoffReply`(2671 用 r.n)回「今天 3 段都收好了」, 跨天回 GRACE_EXPIRED_NOTICE。0.3.1 同样封存(非零影响破坏), 但 0.3.1 路径写死几乎碰不到这个形态。终稿 §4.2 写「finalizeDay 照常(封存行无害)」——实现与终稿一致, 但终稿在共用模式同段落又明说要避免「往昨天的每日笔记里塞封存行」, 两处态度相反, 且与「不动用户内容」冲突。**修法**: fn(3899)开头加与 `_count` 同源的护栏: `if (!this._shared() && isForeignFile(content)) { const i = content.search(HEADER_RE_G); if (i < 0 || !countMessages(content.slice(i))) { status = "empty"; n = 0; afterSeal = 0; return content; } }`, 之后 `n = this._count(content)`(非外来文件 `_count === countMessages`, 老用户字节不变); 零插件内容不落笔、回「今天还没记东西」, 有块时封存行仍按现状追加在文件末尾。终稿 §4.2 改成「段头之后有块才封存, 段数只数段头之后」; B12 加「用户文件无插件块 → finalizeDay empty 且字节不变」「有 1 块 → sealed 且 n===1」。若拍板保持封存, 至少 n 改用 `_count` 让「晚安」与「在吗」一致。

**A7 [DOC-2] 「帮助」回执写死「凌晨 4 点前记的都算前一天」, 而「一天从几点开始」已开放成下拉框, README:51 还建议共用模式填 0。** `HELP_TEXT` 2493 字面, `helpText(shared, heading)` 2621 只拼接常量不看 dayStartHour; 5184 下拉框落盘并 `setDayStartHour`, 5317 onload 按 settings 生效。用户按 README 建议选 0 后发「帮助」, 回执仍说 4 点——插件自己的回执与自己的设置矛盾, 正是目标用户必踩。README:17 也写死 4 点。「设成 0 后关开关该项消失」按终稿 B2 是拍板设计, 「提醒时间」desc 已动态显示当前值, 不算问题。**修法**: `helpText(shared, heading, dayStartHour)`, 那一行按值拼(0 → 「一天从零点切换, 不做熬夜合并」; 其他 → 「凌晨 N 点前记的都算前一天」), 默认 4 输出与 `HELP_TEXT` 逐字相同(【G】黄金回归不动); 调用点 4499 传 `_dayStartHour`。README:17 加「(默认; 可在设置里改)」。

## B 应修

**B1 [Z5, S5, 5, DOC-5] 模板缺失/读失败不够响亮: catch 分支完全静默; Notice 每个路径整个进程只弹一次; desc 没标红; 改对路径后旧警告不清。** 四点全部实证: (1) 3703 `catch (e) { tpl = ""; }`——cachedRead 抛错(同步盘占用、权限、刚被替换)时无 Notice、不设 `_templateMissing`、连 console 都没有, 文件按无模板建, 回执照常; 终稿 §4.3 明写「不存在/读失败」两种都要 Notice + 标红。(2) 3707 `_templateNoticed !== tp` 守卫: 同一错路径连建 3 天文件 notices 只 1 条, Obsidian 常驻几周不重启, 第 2 天起静默按无模板建——正是终稿 §4.3 括号里「否则微信先到的每一天都静默丢模板」要避免的; `_createDayFile` 一天最多跑一次, 守卫没有防刷屏作用。(3) 5153 `tplMissing` 纯文本拼进 setDesc, 无 `.wd-path-preview-bad`, 终稿 §2/§4.3 两处要求「标红」。(4) 5159 onChange 与 selectSuggestion 不清 `_templateMissing`, 清空或改对路径后设置页挂着旧路径警告直到下次成功建文件。微信先到=用户不在电脑前, 5 秒 Notice 本就看不到, desc 才是持久信号, 所以 (3)(4) 比 (2) 更要紧。**修法**: catch 里与「没找到」同款: `this.plugin._templateMissing = tp; console.error("[wechat-diary] 模板读取失败:", tp, e && e.message); new Notice("微信随手记: 每日笔记模板读取失败: " + tp + " (今天的文件已按无模板创建)")`; 守卫改按逻辑日 `_templateNoticedDay !== day`(或直接删掉), Notice 给 10000ms; desc 用 `setting.descEl.createSpan({ cls: "wd-path-preview-bad", text: " ⚠️ 上次建文件时没读到模板文件: " + p })`(styles.css:79 已有该类), 文案「没找到」改「没读到」覆盖两种情况; templatePath 的 onChange 与 selectSuggestion 里 `plugin._templateMissing = ""`。bindtest 加 cachedRead 抛错用例、连续两天两条 Notice 断言。

**B2 [2, DOC-4] 独立模式 + 附件位置「跟随 Obsidian」: 当天第一条是附件时不先建文件, 附件落到库根目录。** 3521 `if (this._shared() && …)` 只在共用模式先建文件; 独立模式 writeImage 3830–3831 / writeAttachment 3565 先 `resolveAttachmentPath` 再 `_appendBlock`, 当天第一条是附件时文件必不存在。从本机 obsidian.asar 抠源码核实: `getAvailablePathForAttachment` 给了 sourcePath 但 `getAbstractFileByPath` 为 null 时 s=null 不回退活动文件, `attachmentFolderPath` 为 `./`/`./子目录` 时落库根/根级 attachments; 脚本实跑图片建在库根 `2026-08-20-2222-0cd0.jpg`。同一天第二张落日记旁边, 附件分两处, 无提示。终稿 §3 只给共用模式补洞, 但它给的理由对独立模式同样成立——代码与终稿一致, 是终稿的洞。内容不丢(wikilink 全路径), 仅 opt-in 配置可达, 默认用户零影响。**修法**: 3521 改成 `if (!vault.getFileByPath(notePath)) { if (this._shared()) await this._createDayFile(day, ""); else { await this._ensureParents(notePath); await vault.create(notePath, ""); } }`(独立模式不能走 `_createDayFile`, 会套 templatePath 模板; `_transform` 3617 对已存在空文件走 process→fn("") 补 frontmatter+段头, 与 create 路径字节相同, bindtest【29】已断言同构); 整段仍只在 `attachmentMode === "obsidian"` 下执行(3517 已守)。终稿 §3 补「独立模式同理先建」, 生产库手测清单②加「独立模式 × 当日文件不存在 × ./子文件夹」。

**B3 [Z3] 默认路径格式改走 moment 后, 界面语言为 ar/fa/hi/bn/ne/mr/my 时路径数字变本地数字, 违反「独立模式每个字节不变」。** 0.3.1 用 `dateStr.slice(0,4) + dateStr` 拼路径永远 ASCII; 0.4.0 3499 `diaryPath` 无条件走 3221 `renderPath` → `moment.format`, format 末尾经 locale postformat 换数字。真 moment 2.29.4 实测: ar 输出 `日记/٢٠٢٦/٢٠٢٦-٠٨-٣٠.md`, fa `۲۰۲۶/…`, zh-cn/en 不变; obsidian-1.13.7.asar 确认按界面语言 `window.moment.locale(gd)`。同一脚本里 3504 `_attachmentDir` diary 分支仍用 `dateStr.slice(0,4)`, 附件与日记分家; frontmatter date 仍 ASCII。升级后当天已有文件找不到(在吗回 0、撤回失败), 新消息写进新路径。人群 = 微信绑定用户 × 阿拉伯语界面, 实际约零, 终稿 §8 已记「语言相关令牌随界面语言渲染」; 产品驳斥者判为极端构造。但修法一行且守住零影响铁律, 归 B。**修法**: 3221 `renderPath` 里 `const bare = format.replace(PATH_LITERAL_RE, ""); return /MMM|ddd/.test(bare) ? m.format(format) : m.locale("en").format(format);`——纯数字令牌(含默认值与 Roy 的 `YYYY/MM/YYYY-MM-DD`)永远 ASCII, 只有带月份/星期名的格式才随界面语言(与终稿 §8 那条正好对应); 或最小改法: `pathFormat === DEFAULT_SETTINGS.pathFormat` 时短路成 0.3.1 字符串拼接。bindtest 的 moment 桩加一个 ar 式 postformat 用例断言默认格式输出不变。

**B4 [6, DOC-7] 「日记文件夹」desc 过时、「填 / = 库根目录」放错位置、改文件夹后路径预览不刷新。** 5040 desc 仍是 0.3.1 原文「按 年/日期.md 存放 (与 Python 版数据契约一致)」——路径格式可配后对改过格式的用户是假话; 终稿 §2 把「填 / = 库根目录」写在 diaryFolder 行, 代码放在 5071 路径格式 desc 末尾; 5052 建议列表 `f.path !== "/"` 排除库根, 用户只能手打 `/`(与终稿无冲突, 保留)。5043 onChange 与 5056–5060 selectSuggestion 只 persist, 不调 `fmtPreview`(5074 才声明, 只挂在格式框去抖里), 改文件夹后预览一直显示旧根目录直到重开设置页——README:22「实时显示今天会写到哪个文件」在这一步不成立。功能没坏, `_root()` 3492 对 `/` 返回空前缀正确。**修法**: 5040 desc 改成「日记的根目录; 填 / 表示库根目录(核心每日笔记的默认位置)。文件放哪由下面的「日记文件路径格式」决定; 打几个字搜索库里的文件夹直接选, 输入新路径会自动创建」; 5071 末尾那句删掉; `let fmtPreview = null` 提到「日记文件夹」Setting 之前, diaryFolder 的 onChange 与 selectSuggestion 末尾 `if (fmtPreview) fmtPreview();`(预览闭包 5076 用 `plugin.writer._root()` 实时取值, 调一下即刷新)。

**B5 [S8] 节标题含「」时 `stripFirstPrefix` 剥不掉开页前缀, 跨天回执同时出现「已自动收尾」和「今天的第一条记录」。** 2610 正则 `「[^」]*」一节了` 在第一个 `」` 后必须接「一节了」, heading=`随手记「工作」` 时无法回溯, 原样返回; `normalizeHeading`(3301–3312)只查空/换行/30 码点, 「」放行。终稿 §4.5 明确两句语义重复只留前者。纯文案冗余, 两句都是真话。**修法**: _dispatch 4625 处改成 `reply.replace(this.writer.firstPrefix(day), "")`(按当前标题构造原串), 删掉 `FIRST_OF_DAY_PREFIX_SHARED_RE`; 或正则改惰性 `「[\s\S]*?」一节了 📖\n`。加 heading 含「」的跨天用例。

**B6 [Z4] `_root()` 新增 `trim()`, 超出终稿「兜底一处不改」范围, 且无记录。** 3491 `String(diaryFolder || "日记").trim()` 是 0.3.1 没有的。obsidian-1.13.7.asar 的 normalizePath 不去 ASCII 空白(只折叠斜杠、去首尾斜杠、空串→"/"、全角空格→半角), 所以 data.json 里 diaryFolder=`" 日记 "` 时 0.3.1 写到「 日记 /2026/…」、0.4.0 写到「日记/2026/…」; `"  "` 时 0.3.1 写到「  /2026/…」、0.4.0 trim 后 normalizePath("")→"/"→_join 折叠成库根。当天已有文件找不到。触发面: 设置页 5043 与 0.3.1 的 4463 都 trim, 文件夹选择器存 folder.path, 只有手改 data.json 可达; 带首尾空格的文件夹名 Windows 本身不可用。产品驳斥者判不必改。**修法(二选一)**: 改回 0.3.1 写法只加 `/` 识别: `const f = this.plugin.settings.diaryFolder || "日记"; return f === "/" ? "" : normalizePath(f);`; 或终稿 §2 diaryFolder 行补「0.4.0 起路径函数内也去首尾空白」并进 CHANGELOG。不要现状这样无记录地改。

**B7 [DOC-8] 终稿 §4.3 说复核带 `_writeGen`, 代码与 D13 用的是新计数 `_mutGen`, 三处口径不一。** docs/15:82「复核带写入代数(`_writeGen`, D10 提醒竞态已有同款)…各自 `_writeGen++`」; 代码 3729/3733 用 `_mutGen`, 3886/3917 各 +1, 普通写入不动; `_writeGen` 另在 4308 `_noteWrite` 每次写成功都 +1; docs/00-decisions.md:560 记的是 `_mutGen`。实现是对的——若按终稿复用 `_writeGen`, Templater 场景里窗口内任何一次写入(恰是连发)都会让复核作废, 后来人按终稿「修正」会改坏。**修法**: 终稿 §4.3 改成「复核带变更代数 `_mutGen`(撤回成功/真封存各 +1; 普通写入不计——否则连发第 2 条就让复核作废); 不能复用 D10 的 `_writeGen`」; 采纳 A2/A5 后同段一并更新为 pending 列表的写法。

**B8 [DOC-3] README:51「凌晨 1 点发的消息会进前一天的每日笔记, 回执里会带日期」——只有当天第一条回执带日期。** 3799/4340/4372/4442 都是 `n === 1` 才挂 `firstPrefix`, 跨天语 4304 只在真封存时挂; 实跑第二条回执「记下来啦~ 今天第 2 段 ✍️」无日期。代码与终稿 §4.5(「其余回执不变」)一致, 是 README 超卖; 「今天第 3 段」本身已在告诉用户它接在同一逻辑日后面。**修法**: README:51 改成「当天第一条的回执会带日期(如「记进 2026-08-29 的每日笔记」), 凌晨发的看一眼就知道落在哪天」。更好但非必须: 共用模式 `hour < dayStartHour` 时所有写入回执带日期, 兑现终稿 §4.4「落错天一眼能看出」; 独立模式不动。

## C 记录在案不修

**C1 [1] 设置页去抖定时器是 display() 局部变量, 重绘不清不冲: 400ms 内打完格式再点另一控件, 页面显示旧值而 data.json 已是新值。** 5073 `fmtTimer` / 5101 `attTimer` / 5134 `headingTimer`; 5095/5130/5184 的 onChange 同步 `this.display()` → 5006 `containerEl.empty()`。脚本复现: 定时器触发后 settings.pathFormat=`[daily]/YYYY-MM-DD`, 输入框仍 `YYYY/YYYY-MM-DD`, 预览仍旧路径, `writer.diaryPath` 已是新路径; 节标题分支在开关刚关时 4931 `_headingCollisionCheck` 不看 sharedDailyNote 仍弹确认框。落盘的是用户刚打的值, 不丢内容, 重开设置页即自愈, 需要手速。**顺手可修**: 把 commit 闭包挂到 `this._pending`, display()/hide() 开头 clearTimeout 并同步冲一次; `_headingCollisionCheck` 开头 `if (!plugin.settings.sharedDailyNote) return;`。

**C2 [4] 外来文件确认框「改回默认」把 diaryFolder 与 pathFormat 一起重置, 正文没列会改哪两项。** 4918–4925 `_beforeImport` 为空时两项同重置; 4920 只说「要改回默认路径吗」。diaryFolder=Journal 的老用户只改 pathFormat 命中护栏, 点「改回默认」后新文件去 `日记/2026/…`, 与 Journal/ 的历史分家(不丢内容, 老文件留原处符合「已有文件不搬」)。终稿 §4.2 原话就是「改回默认路径」, 默认路径 = 两项合成, 实现是合理读法; onOk 后立即 display(), 「日记文件夹」输入框当场显示「日记」, 一格改回即可。**可选**: 确认框正文列出将改的项(只列真正会变的): 「日记文件夹「Journal」→「日记」; 路径格式「…」→「YYYY/YYYY-MM-DD」」; 或从 fmtTimer 进来的只重置 pathFormat, 从关开关进来的两项都重置。

**C3 [S4] 节标题之前有未闭合围栏(用户正在编辑器里敲代码块)→ 找不到节 → 末尾另建一节, 回执虚报「今天的第一条记录」。** 3342–3368 inFence 从文件头一路带到标题; 脚本复现末尾多出第二个 `## 微信随手记`, n=1, 用户闭合围栏后文件里两个同名标题, 「第二条」从此撤不到数不到。终稿 §4.1 明文选择「inFence 时不匹配我们的标题」, 实现一致; 触发是瞬态巧合, 一个字节不丢, 重复标题一眼可见剪一下即可; 反向改成标题匹配不看 inFence 会把 A1 的吞节问题引回来。**记录**: 终稿 §8 加「节标题之前有未闭合围栏时找不到节, 会在末尾另建一节, 不丢内容」; 加一条纯函数用例锁定。

**C4 [S6] 模板在 `## 微信随手记` 下带占位文字时, 插件建文件路径把它替换掉(n=1), 文件已存在路径保留并计入段数(n=2、无开页前缀)。** 3714–3717 用 fn("") 直接 splice。终稿 §2 明写「标题下面留空」, §4.2 规定「节里的内容插件当成自己的」, 所以占位文字本来就归插件, 不是用户内容; 只在用户违反说明时显现, 节外字节不动。**记录**: 终稿 §4.3 与 templatePath 的 desc 写明「模板里该标题下的文字会被第一条消息替换」。若要统一: `_createDayFile` 渲染模板后 locateSection, 有节则 `body = trimBody(fn(trimBody(normalizeNewlines(节正文))))` 再 splice。

**C5 [S9] frontmatter 未闭合时, 往后任何 `---` 行(含插件自己的封存行)都被当成闭合 → 节被跳过 → 末尾另建一节。** 3326–3339 `frontmatterEnd` 3335 找到任意 `---` 即返回; 脚本复现 `---\ntitle: 没闭合\n## 微信随手记\n…\n---\n_(今日封存于 22:00)_\n` → locateSection=null。前提是用户文件 frontmatter 已经写坏, Obsidian 自己也把开头到那行当成非法属性块, 用户立刻看见; 与 Obsidian 解析一致, 不丢字节。审稿人原 fix「遇任何 `^#{1,6} ` 行放弃」不妥(YAML 里 `# …` 是合法注释)。**记录**: 终稿 §4.1 把「闭合行」补一句「= 下一行 `---`, 与 Obsidian 一致」。若要修: `frontmatterEnd` 加 heading 参数, 找闭合途中遇到 `## <heading>` 就 return 0。

**C6 [S10] 文件以 BOM 开头且节标题在首行 → 标题匹配失败 → 末尾重复建节。** 3361 精确比对, BOM 留在首行 line 里。条件: 无模板无 frontmatter 的插件自建文件被外部带 BOM 的编辑器另存(Obsidian 与 Win10 1903 后的记事本都不写 BOM); 有 frontmatter/模板的文件不受影响; `﻿## 微信随手记` 在 CommonMark 里本来就不是标题, 用户在 Obsidian 里也看得到那行不渲染。**可选一行**: locateSection/frontmatterEnd 开头 `const start = content.charCodeAt(0) === 0xFEFF ? 1 : 0`, 前段 slice 自然保留 BOM。

**C7 [DOC-6] README「诚实的限制」与契约 v1.4 都没写独立模式的「外来文件」规则。** 3639–3645 `_count` 只数第一个段头之后; 3868–3873 undo 对外来文件拒绝; README 45–52 五条全以「开了开关之后」为前提; data-contract.md 141–195 没有「无 `source: wechat-diary` = 外来文件」的定义。护栏是终稿 §4.2/§9 B1 拍板, §6 的 README 清单不含这条, 契约管文件格式不管计数; 回执直白, 用户能自救。A3 修完后规则变成「段头之后没有块才拒」, 文案随之。**可选**: README「诚实的限制」加一句「开关关着、路径指向不是插件建的文件时, 照常追加, 但没有微信记的内容时「撤回」会拒绝, 「在吗」只数插件写的」; 契约「路径布局」段补同一句并注明 019 无此规则。

**C8 [DOC-9] 导入按钮失败 Notice 比终稿多一句「用 Periodic Notes 的把它的日期格式抄到上面」, 与 5174 desc「(或 Periodic Notes)」打架。** 4960 原文; 4953 只认 `getPlugin("periodic-notes")?.settings?.daily`, 新版 PN 的 calendarSets 不认, 这点没写进任何文案。人群很小, 指引可照做。**可选**: Notice 改「没找到启用的每日笔记设置(核心「每日笔记」或 Periodic Notes 的 daily), 请手填上面的日记文件夹 / 路径格式 / 模板」; desc 末尾加「Periodic Notes 只认其 daily 设置结构」。

**C9 [DOC-10] 契约 v1.4 两处欠定义: `attachmentFolder` custom 相对哪里没写(代码 3510 相对库根, 设置 desc 5099 写对了); 围栏行定义没写(代码 `FENCE_LINE_RE = /^ {0,3}(```|~~~)/` 允许 ≤3 前导空格, 终稿 §4.1 只说「以 ``` 或 ~~~ 开头」)。** 审稿人把「以 ``` 开头」的出处引成契约:170 是引错, 那句是终稿 §4.1 的; 契约 :170 原文「代码围栏里的 # 行不算标题」与代码不矛盾。读取方不需要知道附件位置(契约自己写了 wikilink 是全路径), 019 也不实现共用模式。**可选**: 契约 154 行加「相对库根目录, 不受 diaryFolder 影响」; 契约 170 / 终稿 §4.1 统一成「围栏行 = 行首至多 3 个空格后接 ``` 或 ~~~ 的行, 每遇一行翻转一次围栏状态」。

## 需要谷雨决定的

1. **A5 [S2] 复核作废口径**: 终稿 §4.3 原话是「撤回/结束/晚安各自 ++ 则复核作废」, 代码逐字照做; 但「第一条 + 晚安」积压批次 + Templater 覆盖会丢第一条且回执失实。是改成「只有被撤的正是这份 body 才作废、封存不作废」(需先改终稿措辞), 还是维持现状记录在案? 若采纳 A2 的 pending 列表重构, 顺手一起改最省。
2. **A6 [Z2] 外来文件的封存行**: 终稿 §4.2「finalizeDay 照常(封存行无害)」与同段「不往昨天的每日笔记里塞封存行」态度相反。是改成「段头之后有块才封存」(推荐, 独立模式老文件字节不变), 还是保持封存但把 n 改成 `_count` 口径消掉「在吗 0 段 / 晚安 3 段」的矛盾?
3. **A3 [Z1] 终稿与 D13 措辞**: 「默认布局老用户永不触发」已证伪, 需把 §4.2 / §9 B1 / 00-decisions D13 第 10 条改成「命令建的空文件被用户先手写过的会触发」, 并决定 `UNDO_FOREIGN_REPLY` 新文案(建议「这个文件里没有微信记的内容可撤, 请到 Obsidian 里手动删」)。
4. **B2 [2, DOC-4] 独立模式先建文件**: 终稿 §3 只给共用模式补洞; Obsidian 源码已核实独立模式同样落库根。是本轮一并放开(默认用户零影响), 还是先进生产库 手测②再定?
5. **B3 [Z3] 数字本地化**: 严格违反「独立模式每个字节不变」但人群约零。修一行(纯数字令牌固定 en locale)还是终稿 §8 补「含数字本地化」四个字记录在案?
6. **B6 [Z4] `_root()` 的 trim**: 改回 0.3.1 写法, 还是保留并在终稿 §2 + CHANGELOG 记录? 二选一, 不要无记录。
7. **C2 [4] 「改回默认」范围**: 两项同重置是终稿合理读法; 要不要在确认框正文列出将改的具体值, 或按入口只重置本轮改动的那一项?
8. **C3 [S4] / C4 [S6] / C5 [S9]**: 三处都是终稿明文选择的副作用, 只需在终稿 §8 / §4.1 / §4.3 记录一句; 若想改代码, 修法已在各条给出。