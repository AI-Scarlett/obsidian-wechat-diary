# 依赖
node_modules/

# 零构建路线: main.js 手写、直接进仓库, 无构建产物

# Obsidian 插件的运行时数据 —— 含 iLink bot_token, 绝对不能进 git
data.json

# 本地测试用的 vault
test-vault/

# 环境
.env
.env.local

# 编辑器 / 系统
.DS_Store
.idea/
.vscode/
*.swp

# 交接文件只留本地 —— 公开仓库, 内部上下文不外发
HANDOFF.md
